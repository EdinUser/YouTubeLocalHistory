# Repository instructions

1. All tests must be implemented, fixed, and run in both Chrome and Firefox. Apply fixes made for one browser to the other while respecting each browser's behavior.
2. Always use `gh` first for GitHub interaction.
3. Do not require reviewers for new commits.
4. Run Chrome tests with elevated rights: sandboxed Playwright can cause system crashes. Tests the user runs in their console are not affected.
